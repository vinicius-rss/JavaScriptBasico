var nome = (prompt("Digite seu nome completo:"));

const quantidadeCaracteres = nome.length;

document.body.innerHTML = `A quantidade de caracteres em seu nome é de ${quantidadeCaracteres} caracteres.`;